class Solution(object):
    def isPalindrome(self, s):
        s = list(s.lower())    
        res_string = ""     
        for i in s:
            if i.isalpha() or i.isdigit():
                res_string+=i           
        return res_string == res_string[::-1]
